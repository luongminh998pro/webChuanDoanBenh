{
    "version": 2,
    "builds": [
        { "src": "install.sh", "use": "@vercel/python" },
        { "src": "app.py", "use": "@vercel/python" }
    ],
    "routes": [
        { "src": "/(.*)", "dest": "app.py" }
    ],
    "scripts": {
        "postinstall": "bash install.sh"
    }
}
